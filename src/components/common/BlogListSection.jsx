import React from 'react'; import { Grid, Card, CardActionArea, CardContent, CardMedia, Typography } from '@mui/material'; import { useNavigate } from 'react-router-dom';
export default function BlogListSection({posts=[]}){ const nav=useNavigate(); return (<Grid container spacing={2}>{posts.map(p=>(<Grid item xs={12} md={4} key={p.slug}><Card><CardActionArea onClick={()=>nav('/blog/'+p.slug)}><CardMedia component='img' height='160' image={p.img || '/assets/placeholder.jpg'} alt={p.title} /><CardContent><Typography variant='h6'>{p.title}</Typography><Typography variant='body2'>{p.excerpt}</Typography></CardContent></CardActionArea></Card></Grid>))}</Grid>); }
